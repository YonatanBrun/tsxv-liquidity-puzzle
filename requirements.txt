# Pinned to the last releases that still ship Python 3.9 wheels — this is what
# stops pip's resolver from backtracking for 20 minutes on an EOL interpreter.
numpy==1.26.4
pandas==2.2.3
scipy==1.13.1
statsmodels==0.14.1
matplotlib==3.9.4
pyarrow==17.0.0
pyyaml==6.0.2
requests==2.32.3
yfinance==0.2.51
tabulate>=0.9.0
python-docx>=1.1.0
